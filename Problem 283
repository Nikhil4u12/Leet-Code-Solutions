class Solution {
    public void moveZeroes(int[] arr) {
     int n = arr.length;
     int idx = 0;

    for (int i = 0; i < n; i++) {
        if (arr[i] != 0) {
            int temp = arr[idx];
            arr[idx] = arr[i];
            arr[i] = temp;
            idx++;
        }
    }
}
}
